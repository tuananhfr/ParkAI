# GPU-optimized requirements for production deployment
# Cài đặt: pip install -r requirements-gpu.txt

# Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
PyYAML==6.0.1
aiohttp==3.9.1
python-multipart==0.0.6

# Computer Vision
opencv-python==4.9.0.80
numpy==1.26.3

# PyTorch with CUDA 11.8 (for NVIDIA GPU)
# Nếu GPU của bạn dùng CUDA 12.1, thay cu118 -> cu121
--extra-index-url https://download.pytorch.org/whl/cu118
torch>=2.0.0
torchvision>=0.15.0

# YOLO
ultralytics>=8.0.0

# Optional: TensorRT for faster inference (NVIDIA GPU only)
# Uncomment nếu muốn inference nhanh hơn 2-3x
# nvidia-tensorrt

# Optional: ONNX Runtime GPU (alternative to TensorRT)
# onnxruntime-gpu

# Monitoring & Profiling (optional)
# psutil  # System monitoring
# py3nvml  # NVIDIA GPU monitoring
